{
  "name": "playlists",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --import tsx --test packages/shared/src/**/*.test.ts",
    "test:watch": "node --import tsx --test --watch packages/shared/src/**/*.test.ts",
    "build": "npm run build --workspace=packages/web"
  },
  "devDependencies": {
    "tsx": "^4.23.0",
    "typescript": "^5.8.3"
  }
}
